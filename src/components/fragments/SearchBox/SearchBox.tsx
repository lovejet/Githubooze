import { memo } from 'react'
import { SearchBoxContainer } from './SearchBox.styled'

const SearchBox = () => {
  return (
    <SearchBoxContainer>
      div
    </SearchBoxContainer>
  )
}

export default memo(SearchBox)
